# Python test suite: unit tests + spec parity with C++ (test_spec/*.yaml).
name: Python tests

on:
  push:
    branches: [main, master, feature/python-refactor]
    paths:
      - 'python/**'
      - 'test_spec/**'
      - '.github/workflows/python-tests.yml'
  pull_request:
    branches: [main, master, feature/python-refactor]
    paths:
      - 'python/**'
      - 'test_spec/**'
      - '.github/workflows/python-tests.yml'

jobs:
  test:
    name: Test (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.9', '3.10', '3.11', '3.12']

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          cd python && pip install -e ".[dev]"

      - name: Run tests (unit + spec parity from test_spec/*.yaml)
        run: |
          cd python && pytest tests/ -v --tb=short

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install and compile-check
        run: |
          cd python && pip install -e ".[dev]"
          python -m py_compile filetimefixer/*.py 2>/dev/null || true
      - name: Quick test run
        run: cd python && pytest tests/ -v --tb=short -q
