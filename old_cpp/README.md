# old_cpp (reference only)

This directory holds the **legacy single-file C++** implementation `FileTimeFixer.cpp`. It is kept for reference and is **not** part of the main build.

For the current buildable C++ implementation, use **cpp/** at the repo root.
